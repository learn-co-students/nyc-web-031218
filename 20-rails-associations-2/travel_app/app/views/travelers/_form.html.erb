<%= render "errors/flash_errors" %>

<%= form_for @traveler do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.collection_check_boxes :city_ids, @cities, :id, :name %>
  <%= f.submit %>
<% end %>

<%= link_to "All Travelers", travelers_path %>
